# ── 打包产物 ───────────────────────────────────────────────────────────────
# sync.sh --pack 生成的快照，每次 2.7 MB。git 本身就是版本历史，
# 不需要再版本化一份压缩副本。
# 快照统一落在 .snapshots/（见 sync.sh 的 pack 段），免得和配置目录混在一起。
*.tar.gz
.snapshots/

# ── install.sh 的备份 ──────────────────────────────────────────────────────
# put() 覆盖已有文件前会存一份 <名字>.bak-YYYYmmdd-HHMMSS。
# 那些是**系统上**的文件，正常不该出现在包里；万一 sync 误收就挡掉。
*.bak-*

# ── 绝不入库的凭据 ─────────────────────────────────────────────────────────
# 这几条是**兜底**，不是主防线：正常流程下它们根本不在 manifest.map 里，
# 所以 sync.sh 不会去收。留着是防手滑 `git add -f` 或以后误加进 manifest。
# 完整边界见 CLAUDE.md「边界：不进这个包的东西」。
storage.key
settings.json
*.pem
*.key
id_rsa*
id_ed25519*
.env

# ── 杂项 ───────────────────────────────────────────────────────────────────
*.swp
*~
.DS_Store
__pycache__/
*.pyc
